{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
    <div class="container">
        <div class="jumbotron">
            <h1>Utilisateur</h1>

            <table class="table table-hover text-justify">
                <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ utilisateur.id }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ utilisateur.email }}</td>
                </tr>
                <tr>
                    <th>Pseudo</th>
                    <td>{{ utilisateur.pseudo }}</td>
                </tr>
                <tr>
                    <th>Password</th>
                    <td>{{ utilisateur.password }}</td>
                </tr>
                </tbody>
            </table>

            <a class="mb-3" href="{{ path('utilisateur_index') }}">Retour to list</a>

            <a class="ml-3 mb-3" href="{{ path('utilisateur_edit', {'id': utilisateur.id}) }}">edit</a>

            {{ include('utilisateur/_delete_form.html.twig') }}
        </div>
    </div>
            {% endblock %}

